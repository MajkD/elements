
- Basic level "editor", click with mouse add and remove tiles
  - Save map
- Package game mac and windows
- Make all tweakables read from file
- Show verson in game from git


- More exact collision system??


//------- CLEANUP

this.collidedTiles[index].debugMark();
this.resetTileStates(this.collidedTiles);
if(this.collidedArea) {
    utils.renderSquare(this.collidedArea.pointA, this.collidedArea.pointB);
  }

this.setDebugStates();
this.resetDebugStates();


//------ BUGS

- Player "jumps/twitches" when walking

//------ BUGS No longer reproducable

- Collision flips out sometimes upon roof collision
